<%= form_for(@cinema_film) do |f| %>
  <% if @cinema_film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cinema_film.errors.count, "error") %> prohibited this cinema_film from being saved:</h2>

      <ul>
      <% @cinema_film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cinema %><br>
    <%= f.text_field :cinema %>
  </div>
  <div class="field">
    <%= f.label :film_id %><br>
    <%= f.number_field :film_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
